import { Badge } from "@coss/ui/components/badge";

export function HeaderBadge(): React.ReactElement {
  return (
    <Badge variant="secondary" aria-label="Synthetic data environment">
      synthetic
    </Badge>
  );
}
